05201002 今井航一

init_frame_buffer関数ではr,g,bをすべて0として画面全体を黒色で塗ることとした。

putc関数ではchar型の文字を受け取ってそれをfont.cを用いて色をつけることにより出力する。

puts関数ではputc関数ではchar*型で受け取った文字列の先頭のアドレスから順位null文字までたどることとしている。

puth関数ではvalueを16でdigits_len回除すことで16進数に変換した。
